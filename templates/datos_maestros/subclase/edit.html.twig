{% extends 'base.html.twig' %}

{% block container %}
    <div class="col-md-4 offset-md-4 col-sm-8 offset-sm-2">
        <div class="table-wrapper">
			<div class="table-header">
				<div class='table-title'>
					<h2>{{ accion }} <b>SubCategoria</b></h2>
				</div>
			</div>

			<div class="table-body">
				{{ form_start(form,{'action':'','method':'post'}) }}
				<div class='form-group'>
					<label>
						Descripción
						{{ form_errors(form.descripcion) }}
						{{ form_widget(form.descripcion) }}
					</label>
				</div>
				<div class='form-group'>
					<label>
						Clase Asociada
						{{ form_errors(form.clase) }}
						{{ form_widget(form.clase,{ 'attr' : { 'class' : 'form-group' } }) }}
					</label>
				</div>
				<div class='form-group'>
					<label>
						Contra Cuenta
						{{ form_errors(form.cuenta) }}
						{{ form_widget(form.cuenta,{ 'attr' : { 'class' : 'form-group' } }) }}
					</label>
				</div>
				<div class="modal-footer">
					<div class="form-group"><a class='btn btn-danger' href="{{ path('subclase_index') }}">Volver</a></div>
					{{ form_end(form) }}
				</div>
			</div>
		</div>

	</div>

    <ul>
        <li>

        </li>
    </ul>
{% endblock %}
